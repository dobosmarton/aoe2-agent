# AoE2 Detection Server Dependencies
# Runs on macOS host (Apple Silicon) for CoreML/ONNX inference.

fastapi>=0.115.0
python-multipart>=0.0.7
uvicorn>=0.30.0
numpy>=1.24.0
Pillow>=10.0.0
pyyaml>=6.0
onnxruntime>=1.17.0
# Optional (macOS only): pip install coremltools
