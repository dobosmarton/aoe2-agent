# AoE2 LLM Agent Dependencies
#
# Windows ARM64 (e.g. VMware Fusion on Apple Silicon):
#   Install Python x64 (not ARM64) for full wheel compatibility.
#   Download from: https://www.python.org/downloads/windows/
#   Choose "Windows installer (64-bit)" — NOT the ARM64 version.

# Screen capture
mss>=9.0.0
Pillow>=10.0.0

# Input automation
pyautogui>=0.9.54
pygetwindow>=0.0.9

# LLM providers
anthropic>=0.84.0

# Data validation
pydantic>=2.5.0

# Logging
structlog>=24.1.0
colorama>=0.4.6

# Retry logic
tenacity>=8.2.0

# Object detection (YOLO)
# If torch install conflicts, install separately first:
#   pip install torch torchvision
#   pip install -r requirements.txt
ultralytics>=8.0.0
